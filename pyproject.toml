[project]
name = "veille-cyber-ia"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0.3",
    "psycopg2-binary>=2.9.9",
    "requests>=2.32.3",
]

[tool.vercel]
entrypoint = "api/app.py"
